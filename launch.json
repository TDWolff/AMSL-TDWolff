{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run AMSL Script",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/loader.js",
            "args": ["${file}"],
            "console": "integratedTerminal",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}